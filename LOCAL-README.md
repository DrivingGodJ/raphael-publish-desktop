# Raphael Publish 桌面版

这是基于 Raphael Publish 制作的本地 Windows 桌面版。应用不依赖常驻的本地网页服务，关闭主窗口后程序即退出。

## 使用方法

1. 双击已解压程序目录中的“Raphael Publish.exe”，或使用自行创建的桌面快捷方式。
2. 点击“项目管理”，再点击“新建项目”；在弹窗中输入项目标题并创建。
3. 回到主页，将 FlowUs 中复制的整篇文章直接粘贴到左侧编辑区。标题、正文和强调段落会自动转换为 Markdown。
4. 点击“更新图片”；程序会扫描正文里的网络图片链接、将图片保存到当前项目自己的“图片”文件夹，并自动把链接替换为本地路径。
5. 后续改图时，只需在正文中粘贴新的图片链接，再次点击“更新图片”。已经下载过的相同链接会直接复用；正文已经不再引用的旧图片会移到 Windows 回收站，项目图片文件夹只保留当前文章正在使用的图片。

## 草稿发布

“发布草稿”不再把正文粘贴到公众号网页，而是直接调用微信官方接口。程序会原样上传 JPG/PNG，不再自动转换或压缩；如果微信提示格式或大小不符合要求，请手动处理图片后再试。首次使用需要填写公众号后台“开发 → 基本配置”里的 AppID 和 AppSecret，并将当前公网 IP 加入公众号 IP 白名单。

AppSecret 使用 Windows 本机加密能力保存到当前账户的应用数据目录，渲染页面读取不到明文，也不会经过第三方发布服务。创建成功后仍需进入公众号后台草稿箱预览并手动发布。

“复制到公众号”使用高保真富文本结构，尽量保留当前主题、列表和段落排版；旁边的“复制纯文本”不携带 HTML 样式。

FlowUs 的“着重块”会根据剪贴板中的原始块类型自动转换为单层 Markdown 引用块（`>`），不会重复叠加引用符号。

界面颜色会自动跟随 Windows 的浅色或深色模式，不需要在应用内手动切换。

## 项目文件

项目统一保存在“文档\\Raphael公众号项目”中。每个项目都有独立文件夹，其中包含：

- `正文.md`：项目正文
- `图片`：本地化后的文章图片
- `project.json`：项目名称、主题和导入信息

在应用中删除项目时，会把整个项目文件夹移入回收站，便于误删后恢复。

## 开源来源

本桌面版基于 MIT 许可的 Raphael Publish 项目制作：

https://github.com/liuxiaopai-ai/raphael-publish
